box: rioki/gcc-cpp
build:
  steps:
    - script:    
        name: make
        code: make
    - script:
        name: make check
        code: make check
    - script:
        name: make dist
        code: make dist
deploy:
  steps:
    - rioki/s3put:
        key_id: $KEY
        key_secret: $SECRET
        url: s3://files.rioki.org/rtest/rtest-latest.tar.gz
        file: rtest-0.1.0.tar.gz
